apiVersion: v1
kind: Pod
metadata:
  name: bad-run-as-root
  namespace: demo
spec:
  containers:
  - name: nginx
    image: nginx:1.27
    securityContext:
      runAsUser: 0
    resources:
      limits:
        cpu: 100m
        memory: 64Mi
